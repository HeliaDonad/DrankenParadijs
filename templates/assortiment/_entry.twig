{% extends "_layout" %}

{% block content %}
    <section class="assortiment-details">
        <img src="{{entry.bannerImage.one().url('assortimentImage')}}" alt="">
        <h2>{{entry.title}}</h2>
        <p class="price" >€{{entry.price}}</p>
        <div>
        <h3><strong>Omschrijving:</strong></h3>
        <p>{{entry.fullText}}</p>
        </div>
        <a class="button btn-more" href="#">In winkelmandje</a>
    </section>
{% endblock %}